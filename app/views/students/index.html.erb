All Students:

<ul>
  <% @students.each do |student| %>
    <li><%= link_to student.name, student_path(student) %><br>
      <%= link_to "Delete", student_path(student), method: "delete" %>
    </li>
  <% end %>
</ul>
